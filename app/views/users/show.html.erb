<h1>User Profile</h1>
<p><%= @user.email %></p>

<h2>Created Events</h2>
<ul>
  <% @user.created_events.each do |event| %>
    <li><%= event.title %> on <%= event.date.strftime("%B %d, %Y") %></li>
  <% end %>
</ul>
